version = 1

test_patterns = [
  "tests/**"
]

exclude_patterns = [
  "demo/**",
  # Throwaway OCR diagnostic scripts (one-off, untyped); not production code.
  "crates/ocr-paddle/scripts/**",
]

[[analyzers]]
name = "python"
enabled = true
